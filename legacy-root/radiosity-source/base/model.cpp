//
// Created by bianzunjian on 2024/3/21.
//

#include "model.h"

void Model::scale2(std::shared_ptr<ModelIO> modelio) {
    // float rr = 0;
    // float radi = 0;
    // auto xvw = modelio->m_xvw;
    // auto vfacetio = modelio->m_vFacetio;
    // auto transfer = modelio->m_scenescale;
    // // for(int i=0;i<modelio->m_npoly;i++)
    // // {
    // //     rr=sqrt((xvw[0]-vfacetio[i]->pcenter[0])*(xvw[0]-vfacetio[i]->pcenter[0])+
    // //             (xvw[1]-vfacetio[i]->pcenter[1])*(xvw[1]-vfacetio[i]->pcenter[1])+
    // //             (xvw[2]-vfacetio[i]->pcenter[2])*(xvw[2]-vfacetio[i]->pcenter[2]));
    // //     if (rr > radi) radi = rr;
    // // }
    // for(int i=0;i<modelio->m_npoly;i++)
    // {
    //     rr=sqrt((xvw[0]-vfacetio[i]->pcenter[0])*(xvw[0]-vfacetio[i]->pcenter[0])+
    //             (xvw[1]-vfacetio[i]->pcenter[1])*(xvw[1]-vfacetio[i]->pcenter[1])+
    //             (xvw[2]-vfacetio[i]->pcenter[2])*(xvw[2]-vfacetio[i]->pcenter[2]));
    //     if (rr > radi) radi = rr;
    // }
    // float delx = 2* radi;
    // float dely = 2* radi;
    // int mxpic = MXPIC;
    // int mypic = MYPIC;
    // float a = std::min(0.5*(mxpic - 2) / radi, 0.5*(mypic - 2) / radi);
    // float c=a;
    // float b = mxpic / 2.0;
    // float d = mypic / 2.0;
    // transfer = {a,b,c,d,delx,dely};
}





